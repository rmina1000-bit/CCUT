// [LOCK-1 2026-08-01] 아직 열리지 않은 관제실 페이지 — **조건**이 안 온 것이지 결함이 아니다.
//
// 왜 한 곳에 두는가: 이 문구를 좌측 메뉴(그룹 묶기)와 각 페이지(상단 고지) 두 곳이 쓴다.
//   같은 사실을 두 곳이 따로 적으면 언젠가 갈라진다 — 오늘 하루 반복해서 걸린 그 구조다.
//   여기가 단일 진실원이고, 나머지는 읽기만 한다.
//
// ★ 삭제하지 않는다. "코드 참조 0 ≠ 쓰레기" — 조건이 오면 그대로 쓴다.
//   접는 것은 숨기는 게 아니라 순서를 낮추는 것이다. 클릭하면 그대로 열린다.
export const LOCKED_PAGES: Record<string, string> = {
  users: "사용자 등록 기능이 생기면 열립니다.",
  support: "문의 접수 창구가 생기면 열립니다.",
  revenue: "결제가 연결되면 열립니다.",
  security: "크래시 리포트가 연결되면 열립니다.",
  analytics: "옵트인 텔레메트리가 생기면 열립니다.",
};

// 법무/수사공조는 성격이 다르다. 조건을 기다리는 게 아니라 **구조적으로 데이터가 없다** —
// 로컬퍼스트라 제출할 자료가 존재하지 않는다. 그건 결함이 아니라 설계이고 판매 포인트다.
// 그래서 '잠김'이 아니라 '해당 없음'으로 따로 적는다. 폐지 판단은 아직 하지 않는다.
export const STRUCTURAL_EMPTY: Record<string, string> = {
  legal: "CCUT은 원본 영상을 보관하지 않습니다. 제출할 자료가 존재하지 않는 것이 설계입니다.",
};

export const isLocked = (tab: string) => tab in LOCKED_PAGES;
