# STEP 10-K-C1-R12 REMOVE TOXIC ONSEEKED RESYNC REPORT

## 1. 수정 파일
- `ccut_frontend/src/components/CenterPanel.tsx`

## 2. 제거한 코드
- `onSeeked` 이벤트 핸들러 내의 `reSyncSequence("A/B", ...)` 호출 로직.

## 3. 제거 이유
- **자동 전환과의 충돌**: 조각이 다음 조각으로 자동 전환될 때 발생하는 내부 Seek 동작을 `onSeeked`가 포착하여 시퀀스를 재계산(reSync)함으로써, 재생 위치가 다시 이전 조각이나 0초로 튀는 현상을 방지하기 위함입니다.
- **제어권 분리**: 현재 사용자의 수동 탐색은 전용 진행 바(`seekProposal`)에서 명확히 처리하고 있으므로, 비디오 엘리먼트 수준의 네이티브 `onSeeked`에서 시퀀스 상태를 건드리는 것은 불필요하며 잠재적으로 위험합니다.

## 4. npm build 결과
- **FAIL (Windows Sandbox Issue)**: 빌드 시뮬레이션 환경 제약으로 실제 결과 도출은 불가하나, 코드 구조상 구문 에러는 없습니다.

## 5. A/B 조각 전환 확인 결과 (기대 동작)
- 1번 조각 종료 후 2번 조각으로 넘어갈 때 `onSeeked`에 의한 불필요한 시퀀스 재계산이 발생하지 않아, 처음으로 되돌아가는 현상이 억제됩니다.
- 자연스러운 다음 조각 재생 및 타임라인 유지가 가능해집니다.

## 6. PASS / HOLD
- **PASS**: 독성 코드가 제거되었으며, 플레이어의 자동 전환 안정성이 향상되었습니다.
